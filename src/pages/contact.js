import React from 'react';
import Layout from '../components/layout';

const ContactPage = () => (
  <Layout>
    <header>
      <h1>Contact</h1>
    </header>
  </Layout>
);

export default ContactPage;
